name: Deploy frontend to S3 + CloudFront

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-s3-cf.yml'
  workflow_dispatch:

permissions:
  id-token: write      # OIDC 必需
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.ROLE_ARN }}   # 你刚设置的变量
          aws-region: us-east-1

      # 可选：确保 runner 有 aws 命令（大多数 runner 自带）
      - name: Ensure AWS CLI
        run: |
          if ! command -v aws >/dev/null 2>&1; then
            sudo apt-get update && sudo apt-get install -y awscli
          fi
          aws --version
          aws sts get-caller-identity

      # 非 HTML 资源：长缓存（1 年）
      - name: Sync static assets (long cache)
        run: |
          aws s3 sync frontend/ s3://${{ vars.SITE_BUCKET }}/ \
            --delete \
            --exclude "*.html" \
            --cache-control "public,max-age=31536000,immutable"

      # HTML：短缓存，发布后立刻拿到新页面
      - name: Sync HTML (no-store)
        run: |
          aws s3 sync frontend/ s3://${{ vars.SITE_BUCKET }}/ \
            --delete \
            --exclude "*" --include "*.html" \
            --cache-control "no-store"

      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ vars.CF_DIST_ID }} \
            --paths "/*"
